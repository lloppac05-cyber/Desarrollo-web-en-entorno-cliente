/**
Buscar un elemento en un array de números.

Métodos: indexOf(), includes().
*/
let arrayBuscar = [1,2,3,4,5];
//saca la posicion en la que se encuentra
console.log(arrayBuscar.indexOf(3));
//devuelve true o false si el elemento se encuentra en el array
console.log(arrayBuscar.includes(6));